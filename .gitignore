# Secrets — never commit
.env
*.env
!.env.example

# Dependencies / envs
node_modules/
.venv/
__pycache__/
*.pyc

# Build output (regenerated by `npm run build`)
frontend/dist/

# Model weights and datasets (large binaries; distribute out-of-band)
*.pth
*.rar
vgg16.pth.zip

# OS / editor
.DS_Store
Thumbs.db

# Build artifacts (deployment zips)
*-deploy.zip
